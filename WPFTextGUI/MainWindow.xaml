<Window x:Class="WPFTextGUI.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:WPFTextGUI"
        mc:Ignorable="d"
        Loaded="Window_Loaded"
        Title="MainWindow" Height="450" Width="800">
    <Grid>
        <ScrollViewer Margin="0,0,0,0">
        <StackPanel x:Name="stckWords" Orientation="Vertical">
        </StackPanel>
        </ScrollViewer>
        <!--<ItemsControl x:Name="itemswords" ItemsSource="{Binding}">
            <ItemsControl.ItemTemplate>
                <DataTemplate>
                    <StackPanel Orientation="Horizontal">
                        <TextBlock Text="{Binding Word}" />
                        <TextBlock Text="{Binding Count}" />
                    </StackPanel>
                </DataTemplate>
            </ItemsControl.ItemTemplate>
        </ItemsControl>-->

        <!--<ScrollViewer Margin="0,417,0,0">
            <TextBlock x:Name="txbInfo" FontSize="20" Height="202" />
        </ScrollViewer>-->
    </Grid>
</Window>
